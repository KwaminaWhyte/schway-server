import React, { Component } from "react";

export default class About extends Component {
  render() {
    return (
      <div>
        <p>info about app</p>

        <p>versioning log</p>

        <p>features (current, upcoming)</p>

        <p>
          feature request (allowing users to vote the feature they want most)
        </p>

        <p>report bugs</p>
      </div>
    );
  }
}
